# cPanel Node.js Application Configuration
# cPanel's Node.js Selector should route requests to the app automatically.
# Keep this file free of hardcoded proxy targets so deployments do not depend on a placeholder port.

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
</IfModule>
